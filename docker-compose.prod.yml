services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "${APP_PORT:-80}:80"
    env_file:
      - .env
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    restart: unless-stopped
